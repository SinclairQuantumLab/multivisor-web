[project]
name = "multivisor-web"
version = "0.1.0"
description = "Sinclair group's IMAQ multivisor web UI server"
readme = "README.md"
requires-python = "==3.11.*"
dependencies = [
    "multivisor[cli,web]==6.0.3",
]
